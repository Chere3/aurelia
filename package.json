{
  "name": "aurelia",
  "private": true,
  "version": "0.2.0",
  "description": "Desktop assistant de productividad personal (Pomodoro Pro + Integraciones + Hard Focus Lock)",
  "license": "MIT",
  "packageManager": "bun@1.0.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/desktop && bun run dev",
    "build": "cd apps/desktop && bun run build",
    "typecheck": "cd apps/desktop && bunx tsc -b",
    "lint": "bun run typecheck",
    "validate": "bun run lint && bun run build"
  }
}
